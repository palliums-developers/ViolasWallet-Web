let initState = {
  detailAddr: "",
  name: "",
  eth: null,
  unreadCount:0
};

let ListReducer = (state = initState, action) => {
    switch (action.type) {
      default:
        return state;
    }
};
export default ListReducer;